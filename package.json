{
  "name": "proppay_final",
  "version": "1.0.0",
  "description": "ล่าสุด",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @tailwindcss/cli -i ./src/css/input.css -o ./src/css/output.css --watch",
    "browser": "cd src && browser-sync start --server --files 'css/*.css, js/*.js, *html' npm init"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.5",
    "tailwindcss": "^4.0.5"
  }
}
